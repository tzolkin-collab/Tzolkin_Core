import {createIcon,providerLogo,paymentInstitutionIcon} from './icons.js';
const el=(tag,text,cls)=>{const n=document.createElement(tag);if(text!==undefined)n.textContent=text;if(cls)n.className=cls;return n;};
const labels={stripe:'Stripe',asaas:'Asaas',email:'E-mail transacional'};
export function setupProductPayments({api,configure}){
 let generation=0,data=null,product=null;const host=()=>document.getElementById('view-product-payments');
 function render(){const root=host();root.replaceChildren();if(!data){root.append(el('p','Carregando pagamentos…','empty-list'));return;}
  const intro=el('section',undefined,'pay-product-hero');intro.append(el('span','OPERAÇÃO COMERCIAL','overview-kicker'),el('h2','Pagamentos e comunicação'),el('p','Processadores, ofertas e avisos financeiros de '+product.name+'.'));root.append(intro);
  const connections=el('div',undefined,'pay-connections');for(const key of ['stripe','asaas','email']){const value=data.connections[key],card=el('article',undefined,'pay-connection'),mark=el('span',undefined,'pay-provider-mark');mark.append(key==='stripe'?providerLogo('stripe'):key==='asaas'?paymentInstitutionIcon():createIcon('mail'));const body=el('div');body.append(el('h3',labels[key]),el('p',key==='email'?(value.provider||'Provedor não definido'):key==='asaas'?'Ambiente '+value.environment:'Cobranças internacionais e assinaturas','detail'));card.append(mark,body,el('span',value.configured?'Configurado':'Pendente','status '+(value.configured?'active':'building')));connections.append(card);}root.append(connections);
  const heading=el('div',undefined,'section-toolbar'),copy=el('div');copy.append(el('h2','Ofertas do produto'),el('p','Preço, processador e automações de e-mail ficam versionados por oferta.','detail'));const button=el('button','Configurar ofertas','primary');button.type='button';button.onclick=()=>configure(product);heading.append(copy,button);root.append(heading);
  const offers=el('div',undefined,'pay-offers');for(const row of data.offers){const offer=el('article',undefined,'pay-offer'),p=row.payload;offer.append(el('span',p.provider==='stripe'?'Stripe':'Asaas','client-chip'),el('h3',p.name),el('p',p.kind==='subscription'?'Assinatura':p.kind==='installments'?'Parcelamento':'Pagamento único','detail'),el('strong',new Intl.NumberFormat('pt-BR',{style:'currency',currency:p.currency}).format(p.amount_minor/100)));const rule=data.rules.find(r=>r.offer_slug===row.slug);offer.append(el('p',(rule?.templates.length||0)+' automações de e-mail','detail'));offers.append(offer);}if(!data.offers.length)offers.append(el('p','Nenhuma oferta configurada para este produto.','empty-list'));root.append(offers);
  const note=el('p','As credenciais são globais e permanecem no servidor. As vendas importadas ainda são consolidadas: o Core não atribui uma transação a este produto sem metadata explícita no Stripe ou Asaas.','pay-scope');root.append(note);
 }
 async function load(next){product=next;data=null;const ticket=++generation;render();try{data=await api('/api/products/'+encodeURIComponent(product.id)+'/payments');if(ticket===generation)render();}catch(error){if(ticket===generation)host().replaceChildren(el('p',error.message,'security-banner'));}}
 return{load,clear(){generation++;data=null;product=null;host()?.replaceChildren();}};
}
